object Testing {
    private const val TEST_MOCKK_VERSION = "1.11.0"
    private const val TEST_JUNIT_VERSION = "4.13.2"
    private const val ANDROIDX_FRAGMENT_TESTING_VERSION = "1.3.3"
    private const val ANDROIDX_CORE_Testing_VERSION = "2.1.0"
    private const val TEST_RUNNER_VERSION = "1.4.0"
    private const val MOKK_VERSION = "1.10.6"
    private const val CORE_TESTING = "2.1.0"
    private const val COROUTINES_VERSION = "1.4.2"
    private const val LIVE_DATE_TESTING_VERSION = "1.1.2"
    private const val ARCH_CORE_TESTING_VERSION = "androidx.arch.core:core-testing:2.1.0"
    private const val TESTING_CORE_VERSION = "1.4.0"
    private const val FRAGMENT_TESTING_VERSION = "1.2.0-rc03"
    private const val JUNIT_VERSION = "4.12"
    private const val KOIN_VERSION = "2.0.1"
    private const val ANDROID_TESTING_VERSION = "1.2.0"

    const val truth = "com.google.truth:truth:1.1.3"

    const val testRunner = "androidx.test:runner:${TEST_RUNNER_VERSION}"
    const val androidCoreTesting =
        "androidx.arch.core:core-testing:${ANDROIDX_CORE_Testing_VERSION}"

    const val junit4 = "junit:junit:${TEST_JUNIT_VERSION}"
    const val junitExt = "androidx.test.ext:junit:1.1.2"

    const val fragmentTest =
        "androidx.fragment:fragment-testing:${ANDROIDX_FRAGMENT_TESTING_VERSION}"
    const val coroutinesTest = "org.jetbrains.kotlinx:kotlinx-coroutines-test:${COROUTINES_VERSION}"

    const val espressoCore = ("androidx.test.espresso:espresso-core:3.4.0")
    const val testRules = ("androidx.test:rules:1.4.0")

    const val testlib_junit = "junit:junit:${JUNIT_VERSION}"
    const val testandroidx_junit = "androidx.test.ext:junit:${ANDROID_TESTING_VERSION}"
    const val testandroidx_runner = "androidx.test:runner:${ANDROID_TESTING_VERSION}"
    const val testandroidx_rules = "androidx.test:rules:${ANDROID_TESTING_VERSION}"
    const val fragments_testing = "androidx.fragment:fragment-testing:${FRAGMENT_TESTING_VERSION}"
    const val androidx_testing_core = "androidx.test:core:${TESTING_CORE_VERSION}"
    const val koin_test = "org.koin:koin-test:${KOIN_VERSION}"
    const val mockk = "io.mockk:mockk:${MOKK_VERSION}"
    const val mockk_android = "io.mockk:mockk-android:${MOKK_VERSION}"
    const val livedata_testing = "com.jraska.livedata:testing-ktx:${LIVE_DATE_TESTING_VERSION}"
    const val coroutines_testing =
        "org.jetbrains.kotlinx:kotlinx-coroutines-test:${COROUTINES_VERSION}"
    const val arch_core_testing = "androidx.arch.core:core-testing:${ARCH_CORE_TESTING_VERSION}"
}